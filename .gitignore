# local Julia depots (package server caches)
.depot/
.julia-depot/
.julia-depool/

# Julia manifest: resolved dependency lock file, regenerated locally via `Pkg.update()`
Manifest.toml
debug/

# benchmark: cloned third-party packages (TensorCrossInterpolation.jl)
benchmark/tci/

# benchmark run artifacts
benchmark/thermalstate/*.log
benchmark/thermalstate/*.csv
